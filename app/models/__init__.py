# __init__.py is used to make the models package, and also to import all models in one place for easier imports in other parts of the application.
from app.models.user import User
from app.models.post import Post, ProductFailScore
